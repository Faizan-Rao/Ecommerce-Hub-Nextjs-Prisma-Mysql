/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
  images: {
    domains: ["cdn.pixabay.com","dummyimage.com", "images.unsplash.com"],
  },
}

module.exports = nextConfig
